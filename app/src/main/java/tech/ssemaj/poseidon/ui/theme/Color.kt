package tech.ssemaj.poseidon.ui.theme

import androidx.compose.ui.graphics.Color

// ── Ocean-depths palette ──────────────────────────────────────────────────
// Surface tones (dark showcase — deep-sea pressure gradient)
val AbyssalNavy        = Color(0xFF050D18)  // background — deepest trench
val DeepBlue           = Color(0xFF0A1929)  // surface — open abyss
val MarineBlue         = Color(0xFF0F2744)  // surface variant — mid-water
val SurfaceBlue        = Color(0xFF142D52)  // outline / elevated card

// Accent tones
val BioluminescentTeal = Color(0xFF00D4E4)  // primary — bioluminescent creature-light
val TridentGold        = Color(0xFFC9A227)  // secondary — ancient gold trident
val NeptuneSilver      = Color(0xFF90AEBF)  // tertiary — seafoam silver

// Decision verdict colors
val AquaAllow  = Color(0xFF00E5A0)  // ALLOW — aqua/seafoam
val CoralBlock = Color(0xFFFF6B6B)  // BLOCK — deep-sea coral

// Text
val TextPrimary   = Color(0xFFE8F4F8)  // near-white, slight blue tint
val TextSecondary = Color(0xFF7BA7BC)  // muted ocean blue-grey

// ── Light scheme (secondary showcase) ────────────────────────────────────
val LightPrimary    = Color(0xFF006577)
val LightSecondary  = Color(0xFF8B6914)
val LightBackground = Color(0xFFF4FAFE)
val LightSurface    = Color(0xFFE8F4F8)
val LightOnBg       = Color(0xFF051923)
