<?xml version="1.0" encoding="utf-8"?>
<poseidon mode="enforce">
    <allow host="aparture.thessemaj.tech"/>
    <deny-path pattern="/blocked/*"/>
    <!-- Hostname-only allow-list: no opt-in CIDR ranges. The native/seccomp tiers
         permit the allowed host via DNS correlation (its resolved IPs are seeded
         into the native cache); bare-IP connects that can't be name-correlated to
         an allowed host are denied. -->
</poseidon>
